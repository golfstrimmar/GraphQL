{
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run start",
    "studio": "cd backend && npx prisma studio",
    "dev": "npm-run-all --parallel frontend backend studio"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
